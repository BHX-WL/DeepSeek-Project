# 更新日志 Changelog

## v0.4.3（2026-09-07）—— 发布构建闭环

### 📦 打包修复
- electron-builder 的 extraResources 会裁掉 qq-sentinel 的 node_modules（ws/transformers）→ after-pack 补拷整棵 node_modules（缺失即终止，杜绝装上连不上的残次包）
- after-pack 增加内置语义模型存在校验（缺失即终止）
- 支持 QQS_USER_DATA 环境变量覆盖数据目录（便携/多开/干净验收）
- 首版完整安装包：ImgOCR Setup 0.2.0.exe ~333MB（含语义模型+全部 v0.4 依赖）

## v0.4.2（2026-09-07）—— 更懂群聊

### 🧠 总结智能化
- AI 档（DeepSeek/Ollama）prompt 升级：点出群聊在玩的梗/谐音/黑话并一句话解释（不确定则不编造）、关联网络热点/实事、氛围一句话
- 突发复盘：识别是否由梗/谐音/热点引爆
- 本地语义档：消息若命中热搜（微博/抖音/B站），报告追加「🔥 关联热搜：疑似在聊 …」

## v0.4.1（2026-09-07）—— 降封号风险

### 🛡️ 降风险（保守化）
- 主动 API 全局节流（默认 250ms 间隔）+ 失败退避；事件被动接收不受影响
- 疑似风控（操作频繁/限流等）自动暂停主动拉取并界面提示「⚠️ 风控暂停」
- 保守默认：公告轮询 60 分钟；历史回拉默认 0（不回拉，靠在线积累）；机器人自动扫描默认关（改手动）
- 设置页新增「🔒 风控与采集克制」面板，可调频率；回拉天数输入范围 0-30
- 免责/隐私条款补充联网仅可选场景说明

## v0.4.0（2026-09-07）—— 本地语义总结 + 关键词监控 + 全量整理

### 🆕 新增
- **本地语义总结**：无 DeepSeek Key / Ollama 时，用内置轻量模型（约129MB，随安装包分发）做语义聚类要点总结，全离线免费
- **关键词监控**：报告页添加监控词，命中 → 时间线“关键词”事件（同词每分钟1条）+ 逐条存档；每份报告自带「关键词命中」统计
- **系统通知**：@全体/公告/冲突/每日汇总 系统通知（可开关、窗口聚焦免打扰）
- **本地 AI（Ollama）**：支持本机 Ollama 做 AI 总结/冲突判定（引擎 auto→DeepSeek→Ollama→本地语义→统计）
- **报告导出**：Markdown / JSON 导出（含消息样本）
- **配置加固**：schema 校验/越界钳制、外部修改热读、保存前 .bak、防原型污染
- **连接韧性**：断线自动重连（指数退避+抖动，界面显示第 n 次）
- **快速上手引导**：首次同意免责后展示（4 步）

### 🔧 修复/变更
- 群列表开关语义修复：空=全部时的“关掉某群”现会落成其余群白名单，不再假全选
- 关键词面板从设置页移至汇总报告页管理
- 汇总报告按日分窗 + 复读提示 + 引擎标识
- 测试体系：node:test 66 项（npm test），含语义引擎纯函数
- 内置模型目录 qq-sentinel/models/ 不入库，打包发布时随安装包内置（约 +130~160MB）



本项目采用语义化版本。每次更新都会在此记录「新增 / 修复 / 变更」。

## v0.2.0（2026-08-19）—— 全家桶发布

### 🆕 新增
- **全家桶整合**：大事汇总器 + 图片识别 + NapCat 魔改 一个安装包，互相调用
- **免责声明**：安装前 NSIS 免责页 + 首次运行强制确认（不同意退出）；应用内「协议中心」（免责/用户协议/隐私政策/开源许可）
- **二维码登录流程**：NapCat 需要登录时，二维码自动弹出到 Windows Photos（绕开 WPS 等劫持）；**弹出前强制风险警告并引导使用小号**；登录成功后自动关闭查看器
- **机器人检测**：扫描群成员 `is_robot`，界面标注「🤖 机器人」，自动并入汇总忽略列表
- **本地统计汇总**：无 DeepSeek Key 或调用失败时自动降级（消息统计 + 关键词 + 活跃成员 + 公告/事件 + 热点关联）
- **OCR 互调桥**：大事汇总器「识别群内图片」调用图片识别的本地 OCR 服务（127.0.0.1:8765，令牌鉴权，仅本机）
- **后台运行**：图片识别默认隐藏驻留托盘；启动时自动拉起大事汇总器（用户第一眼看到大事汇总）
- **首次引导 + 帮助中心**：4 步引导、9 大章节帮助
- **一键安装**：仓库提供 `一键安装.bat`（自动下载安装包），Release 附 208MB 安装包
- **更新日志**：本文件 + 应用内「更新日志」入口

### 🔒 安全与防护
- IPC 发送方校验（safeHandle 全量接入，防 XSS 后调用本地能力）
- 修复 safeHandle 无限递归导致的启动崩溃
- 路径白名单 + gid 纯数字校验（防路径穿越）
- 全部界面文本转义（防 XSS）；CSP 限制；只读 API 白名单
- 日志脱敏（Token / API Key 不落盘）；配置写入白名单；原子文件写入
- 修复 `windowsHide` 连带隐藏 GUI 窗口；统一 userData 目录（%APPDATA%\imgocr）

### ✅ 修复
- NapCat 启动控制台窗口隐藏（移除 `--enable-logging`，日志改走文件 + 「打开 NapCat 日志」）
- 撤回消息不再记录为大事（忽略撤回）
- QQ 升级后 PacketBackend 警告不影响核心功能（图片下载走文件服务）
- 配置 BOM 陷阱（外部编辑 config.json 需无 BOM）

### 📦 打包
- electron-builder 单安装包（共享 Electron 运行时 + 精简 NapCat）
- afterPack 补拷 NapCat node_modules（修复打包后 NapCat 无法启动）

## v0.1.0（2026-08）—— 图片识别工具（开发阶段）

- 本地 OCR（tesseract.js，离线中英文模型），批量/文件夹/单张导入
- 结果 JSONL 本地存档 + 全文搜索
- NapCat 群图拉取（魔改 token 通道下载）
- DeepSeek 文本总结（可选）